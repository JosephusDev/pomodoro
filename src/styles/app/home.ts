import { StyleSheet } from 'react-native'

export const s = StyleSheet.create({
  main: {
    flex: 1,
    padding: 20,
    gap: 20,
    marginTop: 40,
  },
  container: {
  },
  flatList: {
    height: '56%',
  }
})
